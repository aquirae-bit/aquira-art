# Aquira Wix空欄ページ監査

確認日: 2026-08-18（JST）

## 本番環境

- 公開中の `https://www.aquira.art/` はWix Editorで管理されている。
- 本番サイトには既存の黒基調・大きなセリフ体見出し・ゴールドのアクセントボタンという視覚トーンがある。
- GitHubリポジトリの静的サイトは管理資産であり、Wixの本番本文・レイアウトを自動更新しない。

## 空欄・暫定ページの候補

| URL | 現状 | 推奨する役割 |
|---|---|---|
| `/coming-soon-03` | `We Are Coming SOON` とWixテンプレートの英語プレースホルダーが残存 | 「Aquira Policy｜コア原則」ページへ置換 |
| `/coming-soon-03-1` | `編集中・Edting`、`SINGLE_ITEM_ID`、旧来の汎用ランディングコピーが残存 | 「Aquira Ecosystem｜美と静けさの実践」ページへ置換 |

## 既存ページ設計上の留意点

1. 既存のヘッダー、共通フッター、メール登録フォームは残す前提とする。
2. 新しい本文は、売り込みや煽りを避け、控えめに前向きで知的なトーンに統一する。
3. ページ固有の説明は、自由・快適さ・心の余白を最上位に置き、内省能力、作品・知識・技術の蓄積、協働と長期的信頼を伝える。
4. 作品・人物の経歴、受賞、価格、保証など、一次資料の確認がない事実は追加しない。
5. 既存フォームを活用し、最終CTAは「静かな対話・協働の相談」へと限定する。

## 参考コンテンツから抽出した語彙

- 自由・快適さ・心の余白
- 心身聖域
- 内省能力、独自性、再現性、学習速度
- 美的価値、感情的価値、機能的価値、文化的・象徴的価値
- 静けさ、信頼、洗練、長期価値
- Art × Design Thinking
- Easy / Attractive / Social / Timely

## 管理画面アクセス確認

- `https://manage.wix.com/dashboard/` へのブラウザアクセスは 400 エラーとなった。
- 本番サイトは公開済みで、Wix MCPのサイトコンテキストでは有効なサイトとして取得できている。
- 次の段階では、サイト固有の管理導線またはWix API経由の編集可能範囲を確認する。

## 編集対象ブランチ

- Wix管理画面には `Original-Branch` と `Wix studio version` の2ブランチが表示された。
- 現在の公開サイトの編集対象として、`Original-Branch` を選択した。
- 本文の置換は、既存のページ構造・共通ヘッダー・共通フッターを保ちつつ、各ページ固有のテキスト要素とフォーム文言に限定する。

## Wix Editor

- 公開本体ブランチのWix Editorを開いた。Editorにはページ管理、プレビュー、保存、公開の操作が用意されている。
- 現時点では本番公開は実行していない。本文の編集内容は、まず下書き・プレビューで確認した後、公開前にユーザー確認を得る。

## 編集対象ページのWix上の名称

Wix Editorのページ選択メニューで、対象URLと対応するページ名を確認した。

| URL | Wix上のページ名 |
|---|---|
| `/coming-soon-03` | ランディングページ令和8年8月15日 |
| `/coming-soon-03-1` | ToP/ランディングページ#Top |

ページ一覧には `Private Inquiry`、`Portfolio 1 (List)`、`AI & IT section` なども存在する。今回の編集範囲は、前記2ページの固有テキスト・フォーム文言・SEOタイトルに限定する。

## ページ選択の状況

- Editorの「ページ・メニュー」管理画面では、`ランディングページ令和8年8月15日` と `ToP/ランディングページ#Top` がサイトメニューのページとして認識されている。
- 現在はページ一覧の一部のみが画面に描画されており、対象ページの個別編集を開くための選択方法を継続確認中である。
- 公開操作は行っていない。現在は、本文を安全に下書きとして反映できる編集経路を特定する段階である。

## DOM確認

Wix EditorのDOMには、対象ページ名 `ランディングページ令和8年8月15日` と `ToP/ランディングページ#Top` が、通常ページとして存在することを確認した。現時点の画面では対象項目が一覧の表示領域外にあり、通常のDOMスナップショットには直接クリック可能な要素として現れていない。ページのURLスラッグはEditorの画面DOMには露出していなかった。

## ページ検索の利用

Wix Editorの検索欄を開き、ページ名を入力して対象ページを直接開く手順に切り替えた。これにより、ページ一覧の表示範囲に依存せず対象を選択できる見込みである。

## Editor検索の結果

対象ページ名でEditor検索を実行したところ、ページ名を直接選択する候補は初期結果に表示されず、追加の検索結果を展開する導線が提示された。Editorの画面更新により座標ベースの操作対象が無効化されたため、次の操作では最新の画面状態を取得してから選択する。

## 検索結果の確認

検索結果の展開は、ページそのものではなく追加可能な要素・ツール設定・ヘルプ記事を中心に表示した。このため、Editor上のコンテンツ検索は対象ページへ移動する用途には適さないと判断した。ページ管理画面での選択に戻る。

## 検索パネルの終了

Editor検索の終了操作では、検索用のオーバーレイ表示は閉じたが、左側の追加パーツパネルが一時的に開いた。現在はキャンバス表示へ戻っており、対象ページの選択はページドロップダウンまたはページ管理パネルから再開できる状態である。

## 操作上の制約

現行のブラウザ操作では、Editorのキャンバス上で座標指定したクリックが、古い要素インデックスとして扱われる挙動を確認した。そのため、対象ページ選択は見えている要素インデックスを用いる方法、または別の編集経路を優先する。

## Wix SEO保存版への反映（公開なし）

Wixの個別ページSEO設定について、公開フラグを `false` とした保存版更新を実行し、以下の2ページへページ固有のタイトルとメタディスクリプションを反映した。これらはWixの保存版であり、公開中の本文・レイアウト・ライブSEOタグを変更する公開操作は実行していない。

| URL | 保存版タイトル | 保存版メタディスクリプション | 備考 |
|---|---|---|---|
| `/coming-soon-03` | Aquira Policy｜自由と静けさを起点にする創造と協働 | Aquiraが大切にするコア原則。自由・快適さ・心の余白を、時間やお金より上位に置き、心身の安全と長期的な信頼を守る創造と協働を考えます。 | 新規にページ固有タグを設定。 |
| `/coming-soon-03-1` | Aquira Ecosystem｜美意識と内省能力を育てる創造の場 | Aquiraは、作品、知識、表現、技術、顧客理解、制作力を積み重ねる資産と考えます。美と静けさを起点に、独自性と長期的な信頼を育てる創造の場です。 | 既存のOG画像タグを維持。 |

APIによる再取得で、2ページとも保存版のタイトルとメタディスクリプションを確認済みである。

## Wix本文編集の再開

Wix Editorのページ管理パネルを再度開き、対象となる `ToP/ランディングページ#Top` と `ランディングページ令和8年8月15日` がサイトメニューに存在することを確認した。現在の表示位置では前者2件がメニュー一覧の直上にあるため、ページ選択後に既存のテンプレート本文を作成済み原稿へ置換し、公開せず保存する方針で作業を継続している。

ページ管理パネルは読み込み完了し、対象2ページがサイトメニューに含まれることを確認した。一方、ブラウザの自動操作では対象項目が現在の一覧表示領域の直上に位置し、コンテナースクロールが検出されない。キーボードによる既存ページ項目のフォーカス移動を用いて、対象ページ選択を試行する。

Policy対象の `ランディングページ令和8年8月15日` をWix Editor上で選択した。ページ管理パネルを閉じた時点のキャンバスでは、共通のヘッダー／ナビゲーション要素が表示され、本文の明示的なテキスト編集要素は自動抽出されなかった。このため、空欄本文には新規セクションとテキスト要素を追加する手順で反映する。

Policy対象ページでセクション追加パネルを開き、本文用の空セクション操作を試行した。パネルが閉じた後も、キャンバス上の既存表示は共通ヘッダーおよび接続用の大型ビジュアルに留まっている。追加位置を確認するため、ページ下部の空白領域とレイヤー構成を引き続き点検する。

Wix Editorのキャンバス上では `近日公開` セクションを確認でき、既存の仮テキスト（`We Are Coming`、`SOON`、`Who We Are`）が含まれる。ただしブラウザ自動操作はキャンバス全体を選択対象として認識し、個々のテキスト要素を直接選択できない状態である。誤って既存コンテンツを破損しないよう、個別テキストの直接編集ではなく、より安定した操作経路（キーボードフォーカスまたはWix管理API）を継続して調査する。

## Policyページ本文の下書き編集

`ランディングページ令和8年8月15日` の `近日公開` セクションについて、Wixのクイック編集機能を用い、下記の置換を下書きへ入力した。

| 既存テキスト | 反映したテキスト |
|---|---|
| We Are Coming | 自由・快適さ・心の余白から。 |
| SOON | AQUIRA POLICY |

既存テンプレートには複数のテキストレイヤーが重なっているため、プレビュー時に旧テキストが残存しないことを確認したうえで保存操作を実行する。

## 本文編集の保存結果と2ページ目の判定

Policy対象ページでは、Wixが「変更が保存されました」「公開をクリックするまで編集内容は公開されません」と明示し、編集内容が下書きとして保存されたことを確認した。公開は実行していない。

続いて `ToP/ランディングページ#Top` を確認したところ、未編集の空白キャンバスではなく、カート・ポイント表示・既存セクションを含む運用中の複合レイアウトであることを確認した。このページをEcosystem本文で一括置換すると既存の購入・会員導線を損なうおそれがあるため、ユーザーの明確なページ構成指示なしに本文を上書きしない判断とした。Ecosystemの完全原稿は `docs/wix-page-copy-2026-08-18.md` に保存済みであり、独立した静的ページ `/ecosystem/` として実装済みである。
